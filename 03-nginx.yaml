- name: nginx 
  hosts: web
  become: yes
  tasks:
  - name: install nginx
    ansible.builtin.package:
      name: nginx
      state: absent
  - name: install nginx
    ansible.builtin.package:
      name: nginx
      state: started
