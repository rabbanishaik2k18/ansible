- name: check condition number greater than to 10
  hosts: local
  connection: local
  vars:
    my_number: 15
  tasks:
  - name: Print if number is greater than 10
    ansible.builtin.debug:
      msg: "The numaber {{ my_number }} is greater than 10 "
    when: my_number > 10