- name: filters
  hosts: local
  connection: local
  vars:
    fruits:" apple, banana, mango"
  tasks:
  - name: undefinded veriables
    ansible.builtin.debug:
      msg: Hello from | {{ default('ramesh')}}

  tasks:
    ansible.builtin.debug:
      msg: fruits are "{{ fruits | sep(','')}}"
  
